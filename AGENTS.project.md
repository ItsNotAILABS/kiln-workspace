# KILN project instructions (Codex + Grok)

You are working on the **Kiln forge app**, not the Foundry L1 repo.

- **Canonical git:** `ItsNotAILABS/kiln-workspace`
- **This disk:** `E:\KILN` (Grok) or `E:\workspaces\kiln-codex` (Codex)
- **Do not** commit in `E:\repos\KILN` unless you are changing the Solidity registry.
- **Do not** point a custom domain yet. Live preview stays grok.me until `main` is clean.
- **Lanes:** Grok → `grok/work`. Codex → `codex/work`. Never force-push `main`.
- Pocket is a **seeded ecosystem repo** (`itsnotailabs/pocket`). Keep it first-class.
- Read `WORKSPACE.md` before editing.

Grok Build sandbox rules in `AGENTS.md` still apply when running inside `/workspace`.
On the Windows machine, use git lanes above instead of editing a git-less folder.
